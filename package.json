{
  "name": "optimeal-api",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless API for Optimeal AI meal generation.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "node --check api/generateMealPlan.js",
    "build": "npm run lint"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "zod": "^3.24.1"
  }
}
